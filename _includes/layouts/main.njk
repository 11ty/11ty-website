---
title: Eleventy is a simpler static site generator.
layout: layouts/base.njk
templateClass: elv-default
headerClass: elv-header-default
---
{% include "header.njk" %}

{% if config.outdated -%}
<div class="elv-callout elv-callout-warn elv-callout-sticky">This is an <strong>older version of Eleventy</strong>. <a href="https://www.11ty.io/docs/versions/">Full release history</a>. Go to the <a href="https://www.11ty.io/docs/">newest Eleventy docs</a>. You could try <a href="https://www.11ty.io{{ page.url }}">{{ page.url }}</a> although it may not exist.</div>
{% endif -%}
{% if config.prerelease -%}
<div class="elv-callout elv-callout-info elv-callout-sticky">This documentation is for an upcoming version of Eleventy. Go to the <a href="https://www.11ty.io/docs/">latest stable version of Eleventy docs</a> or check out the <a href="https://www.11ty.io/docs/versions/">full release history.</a></div>
{% endif -%}

<main class="elv-layout{% if layoutClass %} {{ layoutClass }}{% endif %}">
	{{ content | safe }}
</main>

<footer class="elv-layout{% if layoutClass %} {{ layoutClass }}{% endif %}">
{% if ignoreSupporters %}{% else %}
	<h2 id="eleventy-is-supported-by">Eleventy is <a href="/docs/supporters/">supported</a> by… <a class="direct-link" href="#eleventy-is-supported-by">#</a></h2>
	{% include "supporters.njk" %}
{% endif %}
{%- if ignoreGitHubButtons %}{% else %}
	<div class="elv-callout">
		{% include "star-on-github.njk" %}
	</div>
{%- endif %}
{%- if ignoreFooterTestimonial -%}
{%- elseif page.url != "/" and not page.url.endsWith("/docs/") and not page.url.endsWith("/docs/testimonials/")  %}
	{%- set randomTestimonial = testimonials | randompick %}
	{%- testimonial randomTestimonial %}
{%- endif %}

	{% include "footer-nav.njk" %}
</footer>
